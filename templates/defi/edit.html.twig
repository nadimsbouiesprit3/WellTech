<!DOCTYPE html>

<title>Edit Defi</title>

{% block body %}
    <h1>Edit Defi</h1>

    {{ include('defi/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_defi_index') }}">back to list</a>

    {{ include('defi/_delete_form.html.twig') }}
{% endblock %}
